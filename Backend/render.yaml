services:
  - name: my-app
    startCommand: |
      # Install Docker
      curl -fsSL https://get.docker.com -o get-docker.sh
      sh get-docker.sh
      # Start your Node.js application
      npm start
